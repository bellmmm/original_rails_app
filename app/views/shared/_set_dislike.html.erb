<%= form_with(model: current_user.dislikes.build, local: false) do |f| %>
  <div><%= hidden_field_tag :product_id, test_product.id %></div>
  <%= f.submit "dislike", class: "btn btn-primary" %>
<% end %>